#include "engine.h"

namespace ns_engine {
	/*void Scene::Update(const unsigned& dt)
	{
		auto sub = Sort();
		for (auto& it : sub)
		{
			it->Update(dt);
		}
	}

	void Layer::Update(const unsigned& dt)
	{
		auto sub = Sort();
		for (auto& it : sub)
		{
			it->Update(dt);
		}
	}

	void Actor::Update(const unsigned& dt)
	{
	}*/

}